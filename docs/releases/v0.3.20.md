# Release Notes - v0.3.20 (Production Ready)

**Date:** 2026-01-01
**Tag:** `v0.3.20`
**Status:** 🟢 Ready for Deployment

## 🚀 Key Features
*   **Solana Integration:** Full support for Solana wallets and token management.
*   **Token-Gated Events:** Backend logic and UI for exclusive events requiring FWB tokens.
*   **Vouch Leaderboard:** Detailed breakdown (Safe/Fun/Hot) in leaderboard API and UI.

## 🔒 Security Hardening (Backend)
*   **CORS:** Strict restriction of allowed origins, methods, and headers in `config/cors.php`.
*   **CSP:** Strict Content Security Policy in production (blocks `unsafe-inline`, `unsafe-eval`).
*   **Environment:** Safe defaults enforced in `.env.example` (APP_DEBUG=false, empty secrets).

## 🛠️ Deployment Instructions

### Frontend (`fwber-frontend`)
**CRITICAL:** Must use `--webpack` flag to avoid Turbopack issues with Next.js 16.
```bash
cd fwber-frontend
npm install
npm run build -- --webpack
```

### Backend (`fwber-backend`)
Use the automated deployment script:
```bash
./fwber-backend/deploy.sh --env=production
```
This script handles:
*   `composer install --no-dev`
*   Database migrations
*   Cache rebuilding (`config:cache`, `route:cache`, etc.)
*   Maintenance mode toggling

## 📝 Changelog
See `CHANGELOG.md` for the full history of changes.
