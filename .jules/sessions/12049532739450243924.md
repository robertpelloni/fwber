# Autonomous AI Execution Protocol

**Session ID:** 12049532739450243924
**Source:** robertpelloni/fwber

## Activity Log

### Agent (Thu, 18 Jun 2026 17:05:33 UTC)

I have performed a comprehensive initialization and project state analysis. 

**Progress Summary:**
1. I reviewed the recent commit history. We are currently on version `v2.3.7: Atmospheric Messaging - Dynamic Chat Environments`.
2. I analyzed the `ROADMAP.md` and `TODO.md` files to see what is missing and needs to be implemented next.
3. According to the `TODO.md`, the next active item for **Phase 10 - Dynamic Identity & Proximity Forum** is:
   * **Quest Verification**: Use ZK-proofs or NFC tags to verify quest completion tasks.
   * **Group Aura**: Aggregate emotions of group chat participants to determine group-wide mood and theme.
4. I have investigated the codebase related to chatrooms and sentiment analysis to prepare for the "Group Aura" feature. Currently, there is a `calculateConversationVibe` method for 1-on-1 matches in `MatchingHeuristicService`, but no group aura computation for chatrooms.

**Question:**
Should I proceed with implementing the **Group Aura** feature for chatrooms by aggregating the emotions of the participants and injecting it into the chatroom API routes, or would you prefer I focus on another feature from the `TODO.md` or `IDEAS.md`?

---

### Agent (Thu, 18 Jun 2026 18:36:30 UTC)



---

### Agent (Thu, 18 Jun 2026 18:38:14 UTC)



---

### Agent (Thu, 18 Jun 2026 18:39:13 UTC)



---

### Agent (Thu, 18 Jun 2026 18:46:01 UTC)



---

### Agent (Thu, 18 Jun 2026 18:51:30 UTC)



---

### Agent (Thu, 18 Jun 2026 18:54:39 UTC)



---

### Agent (Thu, 18 Jun 2026 18:59:56 UTC)



---

### Agent (Thu, 18 Jun 2026 19:05:21 UTC)



---

### Agent (Thu, 18 Jun 2026 19:12:18 UTC)



---

### Agent (Thu, 18 Jun 2026 19:17:54 UTC)



---

### Agent (Thu, 18 Jun 2026 19:26:20 UTC)



---

### Agent (Thu, 18 Jun 2026 19:28:22 UTC)



---

### Agent (Thu, 18 Jun 2026 19:29:19 UTC)



---

### Agent (Thu, 18 Jun 2026 19:30:13 UTC)



---

### Agent (Thu, 18 Jun 2026 19:32:12 UTC)



---

### Agent (Thu, 18 Jun 2026 19:33:05 UTC)



---

### Agent (Thu, 18 Jun 2026 19:33:52 UTC)



---

### Agent (Thu, 18 Jun 2026 19:34:32 UTC)



---

### Agent (Thu, 18 Jun 2026 19:35:23 UTC)



---

### Agent (Thu, 18 Jun 2026 19:36:07 UTC)



---

### Agent (Thu, 18 Jun 2026 19:44:53 UTC)



---

### Agent (Thu, 18 Jun 2026 19:47:11 UTC)



---

### Agent (Thu, 18 Jun 2026 19:48:01 UTC)



---

### Agent (Thu, 18 Jun 2026 19:51:14 UTC)



---

### Agent (Thu, 18 Jun 2026 19:51:56 UTC)



---

### Agent (Thu, 18 Jun 2026 19:52:38 UTC)



---

### Agent (Thu, 18 Jun 2026 19:53:29 UTC)



---

### Agent (Thu, 18 Jun 2026 19:54:13 UTC)



---

### Agent (Thu, 18 Jun 2026 19:55:11 UTC)



---

### Agent (Thu, 18 Jun 2026 19:55:54 UTC)



---

### Agent (Thu, 18 Jun 2026 19:56:37 UTC)



---

### Agent (Thu, 18 Jun 2026 19:57:27 UTC)



---

### Agent (Thu, 18 Jun 2026 20:02:58 UTC)



---

### Agent (Thu, 18 Jun 2026 20:03:49 UTC)



---

### Agent (Thu, 18 Jun 2026 20
UTC)



---

### Agent (Thu, 18 Jun 2026 23:13:15 UTC)



---

### Agent (Thu, 18 Jun 2026 23:13:35 UTC)



---

### Agent (Thu, 18 Jun 2026 23:14:15 UTC)



---

### Agent (Thu, 18 Jun 2026 23:14:35 UTC)



---

### Agent (Thu, 18 Jun 2026 23:14:49 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
* **fwber** is a "privacy-first, proximity-based social operating system" designed to replace swipe-based dating apps with genuine, real-world interactions.
* **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
* **Technology Stack**:
  * **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
  * **Database**: PostgreSQL or MySQL, using Prisma ORM.
  * **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
  * **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
  * **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
  * **Federation**: Moving towards full ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1. **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2. **Federation**:
   * Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
   * Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
   * SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3. **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks.
4. **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5. **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
   * Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
   * Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6. **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
* **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. DreamHost was explicitly retired.
* **Versioning**: A strict versioning protocol requires bumping `VERSION` (plain text) and updating `CHANGELOG.md` upon every significant change.
* **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
* **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes.
* **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured.
* **Testing**:
  * Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
  * Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
  * Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`.

### UI & UX Standards
* High-fidelity "Cyber-Noir" aesthetic.
* Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
* Content is gated using reusable components like `ContentUnlockGate`.
* Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
* Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
* Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
* Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
* Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Thu, 18 Jun 2026 23:15:30 UTC)



---

### Agent (Thu, 18 Jun 2026 23:15:37 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Thu, 18 Jun 2026 23:16:30 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Thu, 18 Jun 2026 23:17:17 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Thu, 18 Jun 2026 23:18:15 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Thu, 18 Jun 2026 23:19:10 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Thu, 18 Jun 2026 23:20:26 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 03:52:55 UTC)



---

### Agent (Fri, 19 Jun 2026 03:53:24 UTC)



---

### Agent (Fri, 19 Jun 2026 03:55:48 UTC)



---

### Agent (Fri, 19 Jun 2026 03:55:48 UTC)



---

### Agent (Fri, 19 Jun 2026 03:55:58 UTC)



---

### Agent (Fri, 19 Jun 2026 03:56:23 UTC)



---

### Agent (Fri, 19 Jun 2026 03:56:33 UTC)



---

### Agent (Fri, 19 Jun 2026 03:58:56 UTC)



---

### Agent (Fri, 19 Jun 2026 04:01:48 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 04:02:35 UTC)



---

### Agent (Fri, 19 Jun 2026 04:07:47 UTC)



---

### Agent (Fri, 19 Jun 2026 04:11:30 UTC)



---

### Agent (Fri, 19 Jun 2026 04:11:57 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 04:12:49 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 04:13:16 UTC)



---

### Agent (Fri, 19 Jun 2026 04:13:38 UTC)



---

### Agent (Fri, 19 Jun 2026 04:15:25 UTC)



---

### Agent (Fri, 19 Jun 2026 04:19:37 UTC)



---

### Agent (Fri, 19 Jun 2026 04:20:35 UTC)



---

### Agent (Fri, 19 Jun 2026 04:21:33 UTC)



---

### Agent (Fri, 19 Jun 2026 04:22:27 UTC)



---

### Agent (Fri, 19 Jun 2026 04:23:17 UTC)



---

### Agent (Fri, 19 Jun 2026 04:24:30 UTC)



---

### Agent (Fri, 19 Jun 2026 04:25:25 UTC)



---

### Agent (Fri, 19 Jun 2026 04:26:57 UTC)



---

### Agent (Fri, 19 Jun 2026 04:27:53 UTC)



---

### Agent (Fri, 19 Jun 2026 04:28:52 UTC)



---

### Agent (Fri, 19 Jun 2026 04:29:44 UTC)



---

### Agent (Fri, 19 Jun 2026 04:30:46 UTC)



---

### Agent (Fri, 19 Jun 2026 04:32:00 UTC)



---

### Agent (Fri, 19 Jun 2026 04:33:07 UTC)



---

### Agent (Fri, 19 Jun 2026 04:34:10 UTC)



---

### Agent (Fri, 19 Jun 2026 04:37:36 UTC)



---

### Agent (Fri, 19 Jun 2026 04:38:50 UTC)



---

### Agent (Fri, 19 Jun 2026 04:39:15 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 04:40:06 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 04:49:06 UTC)



---

### Agent (Fri, 19 Jun 2026 04:50:55 UTC)



---

### Agent (Fri, 19 Jun 2026 04:52:15 UTC)



---

### Agent (Fri, 19 Jun 2026 04:53:00 UTC)



---

### Agent (Fri, 19 Jun 2026 04:56:30 UTC)



---

### Agent (Fri, 19 Jun 2026 05:03:49 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 05:04:43 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 05:05:50 UTC)



---

### Agent (Fri, 19 Jun 2026 05:07:06 UTC)



---

### Agent (Fri, 19 Jun 2026 05:08:00 UTC)



---

### Agent (Fri, 19 Jun 2026 05:09:00 UTC)



---

### Agent (Fri, 19 Jun 2026 05:09:50 UTC)



---

### Agent (Fri, 19 Jun 2026 05:10:57 UTC)



---

### Agent (Fri, 19 Jun 2026 05:15:11 UTC)



---

### Agent (Fri, 19 Jun 2026 05:16:35 UTC)



---

### Agent (Fri, 19 Jun 2026 05:16:47 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 05:17:57 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 05:30:06 UTC)



---

### Agent (Fri, 19 Jun 2026 05:31:55 UTC)



---

### Agent (Fri, 19 Jun 2026 05:33:15 UTC)



---

### Agent (Fri, 19 Jun 2026 05:35:25 UTC)



---

### Agent (Fri, 19 Jun 2026 05:35:49 UTC)



---

### Agent (Fri, 19 Jun 2026 05:36:10 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 05:36:29 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions:

### High-Level Vision & Architecture
*   **fwber** is a "privacy-first, proximity-based social operating system" aiming to replace swipe-based dating apps with real-world interactions.
*   **Core Philosophy**: Zero-knowledge identity, AI-generated avatars before mutual matches, "Local Pulse" feeds, and strong location privacy.
*   **Technology Stack**:
    *   **Backend**: Node.js (ESM), Express.js, TypeScript. Recently migrated from Laravel PHP in Phase 5/6 (v2.0.0-ts).
    *   **Database**: PostgreSQL or MySQL, using Prisma ORM.
    *   **Real-time Engine**: `socket.io` handles live presence (e.g., event maps). This replaced a legacy Laravel Reverb/Pusher setup.
    *   **Frontend**: Next.js 16 (App Router) utilizing React 18, Tailwind CSS, Radix UI, and PWA capabilities.
    *   **Microservices**: A Rust geo-screener service handles high-density H3 spatial indexing (`fwber-geo`) and Redis Bloom Filters.
    *   **Federation**: Moving towards ActivityPub interop, enabling users to interact over the Fediverse (via `/.well-known/webfinger` and Actor/Inbox routes).

### Major Subsystems & Features
1.  **Core Loop**: Registration, ZK-identity, Onboarding, Discovery (Local Pulse), Matches.
2.  **Federation**:
    *   Prisma schema models include `federation_follows`, `federation_inbox`, and `federation_outbox`.
    *   Cryptographic payload signing/verification utilizes 2048-bit RSA keypairs (`public_key`, `private_key` on `users`).
    *   SSRF mitigation is implemented by enforcing HTTPS and blocking key-fetch requests to local/internal IPs.
3.  **Monetization & Economy**: Merchant marketplace, Stripe subscriptions, FWB loyalty token system, and Solana wallet hooks. Stripe integration requires the `stripe` npm package in dependencies to pass TypeScript compilation.
4.  **Real-time Maps**: Frontend (`EventMap.tsx` using `react-leaflet`) hooks into Socket.io to visualize live coordinates.
5.  **AI Integration (`AiWingmanService`, `ContentModerationService`, `SentimentAnalysisService`)**:
    *   Multi-provider logic via `LlmManager` (OpenRouter/OpenAI/NVIDIA NIM).
    *   Features include real-time Tone Translation, Narrative Compatibility explanations, and dynamic Atmospheric/Aura chat styles based on user sentiment.
6.  **Integrations**: Contact synchronization utilizes OAuth 2.0 (Facebook/Google/Outlook) and `UserIntegration`/`SyncedContact` models.

### Operational & Deployment Patterns
*   **Target Topology**: Vercel for Frontend, Hetzner VPS for Backend/DB/Websockets. Kubernetes manifests are available in `docker/k8s/`.
*   **Versioning**: A strict versioning protocol requires bumping the `VERSION` file (plain text) and updating `CHANGELOG.md` upon every significant change.
*   **Database Management**: The repository does not track Prisma migration files locally. Deployments rely on `npx prisma migrate deploy` or `db push`. 
*   **Model ID Types**: The Prisma `User` model uses a `BigInt` for its `id`; all related models must define their foreign keys as `BigInt`. Enums have generally been replaced with direct string values in controllers due to schema changes. Due to these discrepancies, raw SQL (`$queryRaw`) is sometimes used.
*   **Monitoring & APM**: `ApmMiddleware` increments daily request counters in Redis and pushes metrics to Datadog via a queued job asynchronously. Frontend Sentry error tracking is robustly configured. Performance baseline testing is conducted using a Python script in `fwber-backend/scripts/perf/`.
*   **Testing**:
    *   Backend testing relies on Jest configured for ESM (`node --experimental-vm-modules node_modules/jest/bin/jest.js`).
    *   Prisma is mocked using `jest.unstable_mockModule` to bypass eager `DATABASE_URL` connections. Some local suites (e.g., `auth.test.ts`) may hang and require targeted execution.
    *   Frontend verification utilizes Playwright and Python scripts in `fwber-frontend/verification/`. To verify code quality, use `cd fwber-frontend && npm run lint` and `npm run build`.

### UI & UX Standards
*   High-fidelity "Cyber-Noir" aesthetic.
*   Every backend feature must be comprehensively wired to the frontend with detailed tooltips (using `@radix-ui/react-tooltip`) and descriptions.
*   Content is gated using reusable components like `ContentUnlockGate`.
*   Data exports (GDPR compliance) and compliance policies (TOS/Privacy) are fully implemented.

### Agent Directives
*   Proceed autonomously through cycles without stopping, referencing `docs/UNIVERSAL_LLM_INSTRUCTIONS.md` and maintaining robust `TODO.md` tracking.
*   Merge local branches intelligently, synchronize submodules recursively, and always perform full git push/commits upon feature completion.
*   Log progress extremely meticulously in `HANDOFF.md` to relay context across sessions.
*   Refactor only to remove redundancy; do not mutate functional features without cause. Add code comments exclusively for "why," non-obvious algorithms, and trade-offs.

---

### Agent (Fri, 19 Jun 2026 05:36:57 UTC)



---

### Agent (Fri, 19 Jun 2026 05:37:19 UTC)



---

### Agent (Fri, 19 Jun 2026 05:37:35 UTC)



---

### Agent (Fri, 19 Jun 2026 05:38:01 UTC)



---

### Agent (Fri, 19 Jun 2026 05:38:21 UTC)



---

### Agent (Fri, 19 Jun 2026 05:38:44 UTC)



---

### Agent (Fri, 19 Jun 2026 05:39:13 UTC)



---

### Agent (Fri, 19 Jun 2026 05:40:25 UTC)



---

### Agent (Fri, 19 Jun 2026 05:40:49 UTC)



---

### Agent (Fri, 19 Jun 2026 05:44:16 UTC)



---

### Agent (Fri, 19 Jun 2026 05:44:39 UTC)



---

### Agent (Fri, 19 Jun 2026 05:45:08 UTC)



---

### Agent (Fri, 19 Jun 2026 05:45:38 UTC)



---

### Agent (Fri, 19 Jun 2026 05:51:30 UTC)



---

### Agent (Fri, 19 Jun 2026 05:58:51 UTC)



---

### Agent (Fri, 19 Jun 2026 06:03:13 UTC)



---

### Agent (Fri, 19 Jun 2026 06:04:12 UTC)



---

### Agent (Fri, 19 Jun 2026 06:05:25 UTC)



---

### Agent (Fri, 19 Jun 2026 06:06:25 UTC)



---

### Agent (Fri, 19 Jun 2026 06:07:14 UTC)



---

### Agent (Fri, 19 Jun 2026 06:08:25 UTC)



---

### Agent (Fri, 19 Jun 2026 06:10:01 UTC)



---

### Agent (Fri, 19 Jun 2026 06:11:14 UTC)



---

### Agent (Fri, 19 Jun 2026 06:17:32 UTC)



---

### Agent (Fri, 19 Jun 2026 06:18:52 UTC)

I have successfully implemented the "Group Aura" feature and submitted the changes under the branch `feature/group-aura`! 

The commit `v2.3.8: Group Aura Chatrooms` includes the following:
*   Added `calculateGroupAura` to `SentimentAnalysisService` to aggregate the emotional state of all members within a group.
*   Injected `group_aura` into Chatroom and Proximity Chatroom API endpoints (`fwber-backend-ts/src/routes/chatrooms.ts`, `fwber-backend-ts/src/routes/proximity-chatrooms.ts`).
*   Updated `Chatroom.tsx` to display the active "Aura" and apply matching dynamic, transitionary background styles (e.g., Electric, Noir, Calm) based on collective participant sentiment.
*   Updated `VERSION`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md` to version 2.3.8 to reflect these changes.

I'm ready to continue to the next task if you would like me to. Looking at `TODO.md`, the next active item for Phase 10 is:
*   **Quest Verification**: Use ZK-proofs or NFC tags to verify quest completion tasks.

Let me know if you would like me to start working on this next!

---

### Agent (Fri, 19 Jun 2026 06:19:16 UTC)



---

### Agent (Fri, 19 Jun 2026 06:20:44 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 06:20:45 UTC)



---

### Agent (Fri, 19 Jun 2026 06:22:24 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 06:23:34 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 06:33:06 UTC)



---

### Agent (Fri, 19 Jun 2026 06:34:30 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 06:34:30 UTC)



---

### Agent (Fri, 19 Jun 2026 06:35:43 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 06:49:21 UTC)



---

### Agent (Fri, 19 Jun 2026 06:51:16 UTC)



---

### Agent (Fri, 19 Jun 2026 06:52:40 UTC)



---

### Agent (Fri, 19 Jun 2026 06:52:43 UTC)



---

### Agent (Fri, 19 Jun 2026 06:53:28 UTC)



---

### Agent (Fri, 19 Jun 2026 06:53:52 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 06:54:23 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 07:00:12 UTC)



---

### Agent (Fri, 19 Jun 2026 07:03:22 UTC)



---

### Agent (Fri, 19 Jun 2026 07:06:29 UTC)



---

### Agent (Fri, 19 Jun 2026 07:08:34 UTC)



---

### Agent (Fri, 19 Jun 2026 07:09:36 UTC)



---

### Agent (Fri, 19 Jun 2026 07:10:34 UTC)



---

### Agent (Fri, 19 Jun 2026 07:11:52 UTC)



---

### Agent (Fri, 19 Jun 2026 07:15:17 UTC)



---

### Agent (Fri, 19 Jun 2026 07:16:44 UTC)

I have successfully implemented the "Group Aura" feature and submitted the changes under the branch `feature/group-aura`! 

The commit `v2.3.8: Group Aura Chatrooms` includes the following:
*   Added `calculateGroupAura` to `SentimentAnalysisService` to aggregate the emotional state of all members within a group.
*   Injected `group_aura` into Chatroom and Proximity Chatroom API endpoints (`fwber-backend-ts/src/routes/chatrooms.ts`, `fwber-backend-ts/src/routes/proximity-chatrooms.ts`).
*   Updated `Chatroom.tsx` to display the active "Aura" and apply matching dynamic, transitionary background styles (e.g., Electric, Noir, Calm) based on collective participant sentiment.
*   Updated `VERSION`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md` to version 2.3.8 to reflect these changes.

I'm ready to continue to the next task if you would like me to. Looking at `TODO.md`, the next active item for Phase 10 is:
*   **Quest Verification**: Use ZK-proofs or NFC tags to verify quest completion tasks.

Let me know if you would like me to start working on this next!

---

### Agent (Fri, 19 Jun 2026 07:17:06 UTC)



---

### Agent (Fri, 19 Jun 2026 07:18:02 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 07:18:35 UTC)



---

### Agent (Fri, 19 Jun 2026 07:19:50 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 07:20:44 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 07:30:56 UTC)



---

### Agent (Fri, 19 Jun 2026 07:32:31 UTC)



---

### Agent (Fri, 19 Jun 2026 07:32:37 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 07:33:30 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 07:50:21 UTC)



---

### Agent (Fri, 19 Jun 2026 07:52:16 UTC)



---

### Agent (Fri, 19 Jun 2026 07:53:46 UTC)



---

### Agent (Fri, 19 Jun 2026 07:54:47 UTC)



---

### Agent (Fri, 19 Jun 2026 07:58:37 UTC)



---

### Agent (Fri, 19 Jun 2026 07:59:48 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 08:00:47 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 08:01:56 UTC)



---

### Agent (Fri, 19 Jun 2026 08:03:07 UTC)



---

### Agent (Fri, 19 Jun 2026 08:39:13 UTC)



---

### Agent (Fri, 19 Jun 2026 08:39:53 UTC)



---

### Agent (Fri, 19 Jun 2026 08:40:27 UTC)



---

### Agent (Fri, 19 Jun 2026 08:46:30 UTC)



---

### Agent (Fri, 19 Jun 2026 08:47:52 UTC)



---

### Agent (Fri, 19 Jun 2026 08:49:12 UTC)



---

### Agent (Fri, 19 Jun 2026 08:50:24 UTC)



---

### Agent (Fri, 19 Jun 2026 08:55:11 UTC)



---

### Agent (Fri, 19 Jun 2026 08:56:46 UTC)



---

### Agent (Fri, 19 Jun 2026 08:57:04 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026
17 UTC)



---

### Agent (Fri, 19 Jun 2026 11:42:53 UTC)



---

### Agent (Fri, 19 Jun 2026 11:44:14 UTC)



---

### Agent (Fri, 19 Jun 2026 11:46:16 UTC)



---

### Agent (Fri, 19 Jun 2026 11:47:45 UTC)



---

### Agent (Fri, 19 Jun 2026 11:49:01 UTC)



---

### Agent (Fri, 19 Jun 2026 11:50:18 UTC)



---

### Agent (Fri, 19 Jun 2026 11:51:48 UTC)



---

### Agent (Fri, 19 Jun 2026 11:53:21 UTC)



---

### Agent (Fri, 19 Jun 2026 11:54:45 UTC)



---

### Agent (Fri, 19 Jun 2026 11:56:49 UTC)



---

### Agent (Fri, 19 Jun 2026 11:58:41 UTC)



---

### Agent (Fri, 19 Jun 2026 12:00:08 UTC)



---

### Agent (Fri, 19 Jun 2026 12:01:48 UTC)



---

### Agent (Fri, 19 Jun 2026 12:03:18 UTC)



---

### Agent (Fri, 19 Jun 2026 12:04:44 UTC)



---

### Agent (Fri, 19 Jun 2026 12:06:04 UTC)



---

### Agent (Fri, 19 Jun 2026 12:07:32 UTC)



---

### Agent (Fri, 19 Jun 2026 12:09:21 UTC)



---

### Agent (Fri, 19 Jun 2026 12:10:49 UTC)



---

### Agent (Fri, 19 Jun 2026 12:12:21 UTC)



---

### Agent (Fri, 19 Jun 2026 12:14:28 UTC)



---

### Agent (Fri, 19 Jun 2026 12:16:25 UTC)



---

### Agent (Fri, 19 Jun 2026 12:18:02 UTC)



---

### Agent (Fri, 19 Jun 2026 12:22:47 UTC)



---

### Agent (Fri, 19 Jun 2026 12:24:41 UTC)



---

### Agent (Fri, 19 Jun 2026 12:25:01 UTC)

I've successfully finished implementing the "Group Aura" feature and submitted the code on a clean branch `feature/group-aura-chatrooms`!

The `TODO.md` file notes that the next active feature for Phase 10 is:
- **Quest Verification**: Use ZK-proofs or NFC tags to verify quest completion tasks.

Let me know if you would like me to tackle this next, or if there's another priority!

---

### Agent (Fri, 19 Jun 2026 12:26:26 UTC)



---

### Agent (Fri, 19 Jun 2026 12:26:34 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 12:29:07 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 12:39:56 UTC)



---

### Agent (Fri, 19 Jun 2026 12:41:26 UTC)



---

### Agent (Fri, 19 Jun 2026 12:43:26 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 12:45:36 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

### Agent (Fri, 19 Jun 2026 12:55:06 UTC)



---

### Agent (Fri, 19 Jun 2026 12:56:56 UTC)



---

### Agent (Fri, 19 Jun 2026 12:57:20 UTC)

[PROJECT_MEMORY]

Here is a comprehensive summary of the project's architecture, patterns, and decisions based on my exploration and work within the codebase:

### 1. High-Level Vision & Core Philosophy
*   **Identity & Discovery**: `fwber` is a privacy-first, proximity-based social operating system. It eschews traditional swipe-based dating mechanics in favor of "Local Pulse" feeds, ZK-identity (zero-knowledge verification), and AI-generated avatars that mask real photos until mutual trust is established.
*   **Evolution**: The project's vision (`docs/VISION.md`) has been shifting from a pure proximity dating app to a broader "Local Social Economy," incorporating merchants, community boards, and local quests.

### 2. Technology Stack & Architecture
*   **Backend (`fwber-backend-ts`)**: A Node.js (ESM) + Express.js backend written in TypeScript. It recently underwent a massive "Great Migration" (v2.0.0-ts) from a legacy Laravel PHP architecture.
*   **Database**: Managed via Prisma ORM. Compatible with MySQL (production) and PostgreSQL.
*   **Frontend (`fwber-frontend`)**: Built with Next.js 16 (App Router), React 18, Tailwind CSS, and Radix UI primitives. It supports PWA capabilities.
*   **Real-time Capabilities**: Replaced legacy Laravel Reverb/Pusher with `socket.io` (backend) and `socket.io-client` (frontend) for chat, typing indicators, and live event map location broadcasting (`react-leaflet`).
*   **Microservices (`fwber-geo`)**: High-density spatial indexing and proximity filtering are offloaded to a custom Rust microservice utilizing H3-js (hierarchical hexagonal indexing) and Redis Bloom Filters.

### 3. Major Subsystems & Domain Logic
*   **Federation Engine**: The platform acts as a Fediverse node using ActivityPub. It supports WebFinger, Inbox/Outbox handling, and cross-instance follows.
    *   **Security**: Uses 2048-bit RSA keypairs (generated async via `CryptoService`) and implements strict SSRF mitigation (enforcing HTTPS, blocking internal IPs like `10.*` and `192.168.*`) in `FederationService`.
*   **AI Integration & Intelligence**: Driven by `LlmManager` with multi-provider failover (OpenRouter, OpenAI, NVIDIA NIM). AI capabilities include:
    *   `AiWingmanService`: Proactive nudges, Tone Translation (flirty, confident, etc.), and Profile Roasts.
    *   `SentimentAnalysisService`: Computes single-user emotion, neighborhood vibe, `calculateConversationVibe` for 1-on-1 matches, and the newly added `calculateGroupAura` for dynamic chatroom styling.
    *   `ContentModerationService`: Uses LLM checks with a strict local regex fallback for immediate protection if AI is unavailable.
*   **Economy & Monetization**: Includes FWB token rewards (e.g., for quests or referrals), Stripe integration for subscriptions, and Solana wallet hooks. *Note: Stripe npm package is required for TypeScript compilation in Hetzner CI/CD.*
*   **Matching Engine**: OkCupid-style geometric-mean compatibility heuristic based on user-weighted values, overlaid with proximity history (80/20 weighting) to generate "Narrative Compatibility" reports.

### 4. Database & ORM Patterns
*   **`BigInt` Heavy**: The `users` table utilizes `BigInt` for the primary key `id`. All relational models (like `user_integrations`, `messages`, `matches`) must use `BigInt` foreign keys. This requires careful parsing in frontend/JSON boundaries (handled via a custom `serialize` utility).
*   **Migrations**: The repo explicitly does not track Prisma migration files (`prisma/migrations`) locally. Live deployments rely on `npx prisma db push` or `npx prisma migrate deploy`.
*   **Enums**: Former Prisma Enums (like `UserRole` and `Tier`) have been replaced with direct string values in controllers due to schema changes. Complex queries (like journal entry counts) sometimes resort to `$queryRaw`.

### 5. Testing & Verification
*   **Backend**: Uses Jest + Supertest. Because it's an ESM project, Jest must run with `--experimental-vm-modules`. Prisma connections are mocked using `jest.unstable_mockModule` to avoid eager DB connections. Some local tests (e.g., `auth.test.ts`) are known to hang.
*   **Frontend & E2E**: Verified using `cd fwber-frontend && npm run lint` and `npm run build`. Playwright/Python scripts reside in `fwber-frontend/verification/` for UI snapshots and data compliance checks.

### 6. Operational & AI Agent Guidelines
*   **Master Protocol**: All agents must strictly adhere to `docs/UNIVERSAL_LLM_INSTRUCTIONS.md`.
*   **Continuous Execution**: AI agents are instructed to run continuously and autonomously, selecting features from `TODO.md` and logging extreme detail in `HANDOFF.md` between sessions.
*   **Git & Versioning**: Every completed feature demands a strict version bump in the root `VERSION` file, `VERSION.md`, `CHANGELOG.md`, `ROADMAP.md`, and `TODO.md`. The commit message must include this version.
*   **Sanitization Rules**: The `.gitignore` aggressively excludes build artifacts (`dist`, `.next`, `node_modules`) to avoid massive Git diffs that crash AI context windows. AI agents must intelligently merge local feature branches into `main` before starting new work.
*   **Agent Safety**: Never execute commands that kill all node processes generically (e.g., `killall node`), as this will kill the AI agent's own execution session.

---

