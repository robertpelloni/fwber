#!/bin/bash
sed -i 's/- \[ \] \*\*Outbox Sync\*\*: Hook into internal app actions (e.g., posting a bulletin or changing a bio) to distribute `Update` and `Create` payloads to followers./- [x] \*\*Outbox Sync\*\*: Hook into internal app actions (e.g., posting a bulletin or changing a bio) to distribute `Update` and `Create` payloads to followers./g' TODO.md
